# Rust build artifacts
/target
**/*.rs.bk

# Profiling / coverage output (dhat, cargo-flamegraph, perf, valgrind, llvm-cov, tarpaulin)
dhat-heap.json
*.profraw
*.profdata
perf.data
perf.data.old
flamegraph.svg
massif.out.*
callgrind.out.*
lcov.info
tarpaulin-report.html
cobertura.xml

# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon
._*
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# Linux
*~
.directory
.Trash-*
.nfs*
.fuse_hidden*

# Editors / IDEs
.vscode/
.idea/
*.iml
.fleet/
*.swp
*.swo
*.swn
*.un~
Session.vim
.netrwhist
\#*\#
.#*

# Generated mdBook output (source lives in docs/src/, built via docs/book.toml)
/docs/book/

# Local RRID template checkouts (svn working dirs created at runtime)
SUSE:*/

# Release artifacts produced by `cargo xtask package` (built in CI, not committed)
/dist/release/
# .deb/.rpm staging for the release workflow's package job, plus the listings
# its check step writes there — build.rs stamps `git describe --dirty`
/dist/packages/

# cargo-fuzz build state (fuzz/ is a detached crate; its lockfile, corpus and
# crash artifacts are machine-local)
/fuzz/target/
/fuzz/corpus/
/fuzz/artifacts/
/fuzz/coverage/
/fuzz/Cargo.lock
