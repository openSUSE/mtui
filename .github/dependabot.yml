---
# Dependabot keeps Cargo deps (updating both Cargo.toml and Cargo.lock) and
# GitHub Actions versions current.
# See https://docs.github.com/en/code-security/dependabot/dependabot-version-updates/configuration-options-for-the-dependabot.yml-file
version: 2
updates:
  - package-ecosystem: "cargo"
    directory: "/"
    target-branch: "main"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    groups:
      cargo-minor-and-patch:
        update-types:
          - "minor"
          - "patch"

  - package-ecosystem: "github-actions"
    directory: "/"
    target-branch: "main"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    # dtolnay/rust-toolchain is pinned to a master-branch commit SHA and the
    # toolchain is selected via its `toolchain:` input; the action has no
    # semver releases for Dependabot to bump to.
    ignore:
      - dependency-name: "dtolnay/rust-toolchain"
    groups:
      actions-minor-and-patch:
        update-types:
          - "minor"
          - "patch"
